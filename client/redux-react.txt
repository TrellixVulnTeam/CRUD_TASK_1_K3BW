#làm việc với redux thông qua hooks

-useSelector()
-useDispath()


1. Set up redux store
    - reduxcers $ root reduxcer
    - Action creators
    - Store

2. Setup Redux provider

    - allow redux store to be accessible from anywhere of the app.

3. connect to redux store from component
    - using the twe hooks